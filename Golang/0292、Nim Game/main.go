func canWinNim(n int) bool {
    //要想让我赢，那我必须要给对方留4个石头
    //对方前一步只能给我留5-7个石头
    //再前一步我只能给对方留8个石头
    //前一步对方只能给我留9-11个石头
    //再前一步我只能给对方留12个石头
    //很明显，4的倍数，我抓完之后只要是4的倍数就行了
    
    if n <= 3 { 
        return true
    }

    return  (n-1) %4 ==0 || (n-2) %4 == 0 || (n-3) %4 == 0
    
}
